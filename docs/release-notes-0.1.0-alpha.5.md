# OpenCodeThemeSwitcher 0.1.0-alpha.5（Alpha 体验版）

> **状态：候选登记完成（2026-09-23）**。`npm run release:build` 12 步 `ALL_GREEN`，
> 登记后核验 `RELEASE_GREEN`、`publishable=true`；GitHub pre-release 待 jc 网页操作。
> 发布执行记录见 `docs/alpha-acceptance.md` §9.8。

## 发布物

| 项 | 值 |
|---|---|
| buildId | `20260923081342-001342d-30fc1c` |
| 分发 zip | `candidate-20260923081342-001342d-30fc1c.zip`（127.2 MB，82 条目） |
| zip SHA256 | `addb45120d265cf7de84d2bace5f7dfbe62404363849b4b914580962086ba1b9` |
| exe SHA256 | `ed0f8595ffb62cca7d2e373f5626336af8badacd2269c720579df914b61456f3` |
| app.asar SHA256 | `743b37bb9a5b3379bfef4ac34950438e879a705608c24d19ff7e82029490a869`（968 条目 / unpacked 7） |

哈希侧车 `candidate-<buildId>.zip.sha256.txt` 与 zip 一同分发，不在 zip 内，且**先于上传生成**。

> **非官方工具**。它通过修改 OpenCode 桌面版的本地应用归档来换背景图，
> 不是 OpenCode 官方功能，也不受官方支持。**未签名**，Windows 可能弹出安全提示。

## 相对 alpha.4 的变化（基线门分类放行轮 B1–B3）

- **接管后 OpenCode 自动更新不再恒被拒（核心修复）**：alpha.4 及之前，首次接管后目标
  一自动更新，重应用就被基线门一票否决（「与首次接管快照不一致」），且无自助重新接管入口。
  本候选起：工具把不一致**分类**——版本变化、锚点唯一、变更集合干净、差异达整体替换量级、
  原生模块集合一致**五项同时满足**才判定「官方整体更新」，自动重新接管并继续应用
  （进度与操作记录可见「检测到目标已更新至 X.Y.Z，已自动更新基线」，旧基线按序号归档永不删除）。
- **局部异常改动仍会被拒**：只改个别文件、锚点消失、第三方主题层占用等，维持拒绝；
  文案区分「疑似第三方改动」与「疑似官方更新但未达自动放行条件」两类，各给对应处置建议。
- **判定是结构级、非密码学校验**：它防的是误伤与低成本的顺手篡改，不防蓄意伪造整体更新的
  高成本攻击；五条件宁拒勿纵。
- 真机复验：本修复已在真实安装（1.18.31→1.18.32 自动更新场景）复验通过
  （2026-09-23，见 `docs/alpha-acceptance.md` §6 落账）。
- 其余为非用户可见改动：live-cli 补结构确认通道、logo 素材入库、文档口径联动。

## 这个版本能做什么

给 OpenCode Desktop 换一张本地背景图：从图片里取色，自动推导一整套界面配色，
调好遮罩与面板透明度后写入安装。写入前自动备份，随时可以恢复。

## 兼容范围（只声称验过的）

| 项 | 范围 |
|---|---|
| 系统 | **Windows x64**（开发与测试都在此完成；**没有**在 Windows 11 上实测，不声称支持） |
| 目标（完整验证） | **OpenCode Desktop 1.18.29**，用户级安装（`%LOCALAPPDATA%\Programs\@opencode-aidesktop`） |
| 目标（结构验证通道） | 未列入白名单的版本，仅当结构验证通过并可显式确认才可应用；真机 1.18.31/1.18.32 实测结构兼容（锚点唯一、变更集合干净），但**未做完整行为验证** |
| 图片 | 静态 **PNG**、**JPEG**（`.jpg` / `.jpeg` / `.jfif` / `.jpe`）、**WebP**；≤ 20 MiB，≤ 40 MP |
| 不做 | 动图（动画 WebP / APNG）、GIF、AVIF、TIFF、BMP、HEIC、SVG |

升级 OpenCode 后定制会失效（自动更新同样会覆盖），需要重新应用；
重新应用前工具会自动重验结构，官方整体更新会走自动重新接管（见上），
版本大改导致落点变化时会拒绝而不是写坏。

## 五步使用

1. **完全退出 OpenCode**：不是关窗口 —— 检查托盘区（时间旁边的小箭头）里有没有它的图标，
   有就右键退出。运行中工具会拒绝写入（这是保护，不是故障）。
2. **选图或拖图**：支持上面列出的格式；扩展名只用于筛选，格式按实际内容识别。
3. **调滑杆**：把「背景遮罩」和「面板不透明度」调到字看得清。面板不透明度的调法：
   调高 → 文字更清楚、背景更淡；调低 → 背景更明显、文字风险更高。
4. **应用**：非白名单版本（徽章「结构验证通过」）会先弹逐项结构检查确认框，
   核对后进入应用确认框——列出安装位置、版本、将要修改的条目、备份位置与所需磁盘空间
   （约「归档 ×4 + 64 MiB」）。确认后写入，写入完成会用 hash 复核。
   若目标在接管后自动更新过且满足自动重新接管条件，进度里会告知「已自动更新基线」。
5. **重启 OpenCode 再看效果**：可以直接点顶栏的「启动 OpenCode」，不必手动找图标。

## 怎么恢复

右侧「恢复」区最多三个入口，按你机器上实际有哪些快照出现：

- **恢复上一主题** —— 回到上一次应用之前的样子，保留最近 3 份。
- **恢复到首次接管时** —— 回到本工具第一次接管该安装时的状态。
  **它不承诺是出厂界面**：接管时安装可能已被别的工具改过；目标自动更新并重新接管后，
  该快照仍是旧版本的归档，恢复到它会回到旧版本画面。
- **恢复原版** —— 只在登记过该版本出厂指纹、且备份与指纹吻合时才出现；
  **当前指纹表为空**，所以正常不会看到这个入口。

要回到真正的出厂状态，请重新安装 OpenCode。
恢复后同样**完全退出并重启**再确认。

备份默认在 `%LOCALAPPDATA%\OpenCodeThemeSwitcher\instances\<实例>\backups\`。
注意：**删掉工具目录不等于撤销主题**（归档里的定制还在），同时会失去恢复能力。

## 已知限制（如实列出）

- 未签名；没有代码签名证书。安全软件可能拦截，请记录提示内容，**不要关闭系统防护**。
- 基线门的自动重新接管是**结构级判定，非密码学校验**；不满足五条件的更新（如官方大改
  布局）仍会拒绝，需要等本工具适配后重试，不要按旧快照强行降级。
- 完整真机验证只做过一个 OpenCode 版本（1.18.29）；其余版本走结构验证通道放行，
  通过只意味着「注入机制结构适用」，不承诺界面效果逐项正常。
- 未在干净的非开发环境验证过（A6 按用户决定跳过，平台声明仅开发机 Windows x64）。
- 便携包约 133 MB（未压缩约 341 MB），因为内含 Electron 运行时。
- 背景资源在归档里固定命名为 `oc-theme-background.jpg`，非 JPEG 内容也落在这个名字下
  （按原图字节写入，未做统一转码）。
- 终端（`.xterm`）与 `--syntax-*` / `--markdown-*` 语法高亮不在覆盖范围内（有意为之）。
- 真机走查遗留一处 P3 外观缺陷：主题下顶部栏右侧色块尺寸观感不佳（不影响功能与可读性）。
- 真机视觉走查：U1–U3 顶栏与 1.18.32 重新接管后的观感已由使用者**整体回执**通过
  （2026-09-23），**非逐项**；工具会模拟预览，但**预览不等于真实窗口**。
- 本便携包未做 Electron fuse 加固（RunAsNode 等均为默认值），且未签名。

## 出问题了怎么反馈

给这几项就够（**不需要**发图片、聊天记录或完整用户目录）：

1. 工具版本（`0.1.0-alpha.5`）与候选包 SHA256（见与 zip 一同分发的 `*.zip.sha256.txt`，不在 zip 内）；
2. 报错码与状态栏那行中文提示（原文照抄）；
3. 操作时间（精确到分钟）；
4. 目标 OpenCode 版本，以及徽章是白名单绿还是「结构验证通过」蓝；
5. 若界面显示「待恢复」，把它给出的操作 ID 一并附上。

日志目录：`%LOCALAPPDATA%\OpenCodeThemeSwitcher\`，记录的是路径与哈希，
不含图片内容、也不含你与 AI 的对话。
