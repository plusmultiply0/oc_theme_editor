# A5 真机验收：准备与执行清单

状态：**已授权，等待 jc 完成第 1 步（保存并完全退出 OpenCode）后由 wb 继续执行**。
本文件只记录只读现场与操作步骤，不写入真实安装。

## 1. 现场记录（2026-09-13 只读采集）

| 项 | 值 |
|---|---|
| 目标安装 | `C:\Users\ylzho\AppData\Local\Programs\@opencode-aidesktop\resources\app.asar` |
| 归档大小 / SHA256 | 150,610,355 字节 / `17092e8efa923927095132e42614227aa738ba95aa68272012a784f9b3519746` |
| 归档条目 | 6996 |
| 当前是否已挂本工具 | 是 —— 链接恰好 1 个、标记唯一、位于 head 内、CSS 条目存在（只读核验全 OK） |
| 备份（本工具登记） | `original` 1 条（2026-09-11，`pristine=false`，归档备份 152,395,856 字节）；`previous` 9 条，归档备份若干 |
| **恢复入口预期** | 因 `pristine=false`，界面**不会**出现「恢复原版」；可用的是「恢复上一主题」与「恢复到首次接管时」 |
| OpenCode 进程 | 采集时未运行 |
| 候选包 | `candidate-OpenCodeThemeSwitcher-0.1.0-alpha.1.zip`，131 MB，SHA256 `9212617177fa9bb7c8f46b5bd313f7c76484f26e2c646d5e9e9de598c7e81c6c` |

## 2. 你要做的（授权部分）

1. **保存 OpenCode 里的工作，然后完全退出 OpenCode**（托盘图标也要退，不要只关窗口）。
   我不会强杀进程；未退出时工具会拒绝写入并提示「目标应用正在运行」。
2. 退出后回我一句，我开始执行下面的步骤。

## 3. 我来做的（按顺序，每步记录证据）

1. 复核现场：目标版本、安装路径、归档哈希、备份健康、候选包哈希（已就绪，见上表）。
2. **应用静态 JFIF 壁纸 A** → 提示完全退出并重启 OpenCode → 检查：真实背景、侧栏、正文、
   输入、菜单、tooltip/dialog/Portal、终端与代码区。
3. 完全退出后应用**透明 PNG B**，再应用**静态 WebP C**，每次重启核对效果与图片 hash；
   同 C 重复应用应为 no-op。
4. 切换官方主题、深浅模式、旧/新布局与窗口尺寸，测试 125% / 150% 缩放；
   核对「减少透明度」下实底是预期行为。
5. 恢复上一主题 → 再恢复到首次接管快照；核对归档 hash、能否启动、背景与会话/终端可用。
   最终停在哪个主题由你决定。
6. 保存脱敏截图与**仅布局/样式信息**的 DOM 探针、操作记录，并标注「真实窗口，非 fixture」。

## 4. 停止条件（出现任一情况立即停）

- 无法启动、背景图错误、恢复失败、关键区域不可读。
- 此时：保留现场，走已验证的恢复流程；**不**执行绑定旧事故 hash 的历史恢复脚本。

## 5. 已知限制（不因 A5 而改变）

- A6（干净非开发环境）按你的决定跳过 → 发布声明不得声称「已在干净环境验证」。
- 演示素材未拍摄；真机视觉走查结论以 A5 记录为准。
- A8 当前仍为 NO-GO，A5 完成后再评估。
