# 目标

复审实际项目 OpenCode_Theme_Switcher，核对上轮 R1–R5 修复，保存新问题与可执行方案；不改业务代码或真实安装。

## 阶段

- [x] 技能、路径、Git 初查。
- [x] 阅读 R1–R3 提交及未提交 R4 改动，复核调用链。
- [x] 安全定向测试与隔离负例验证。
- [x] 写审查报告。
- [x] 保存到项目新的 handoff 目录并核对哈希/源码漂移。

## 约束

基线 HEAD 33925dd，tools/release-build.cjs 和 tools/test-release-gate.cjs 已有用户未提交改动，保留不动。先在可写工作区暂存报告，交付时经权限工具复制到实际项目目录；不创建不存在的嵌套目录。

## 错误记录

- Git 读取用户全局 ignore 文件权限警告；状态命令仍有效，不修改配置。
- Windows rg 路径通配报语法错误一次；已改为目录配合 -g 文件过滤。
