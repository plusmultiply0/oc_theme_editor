# 现状审查与计划列表（2026-09-23）

> 基线：HEAD `952708e`。alpha.4 已发布（含 U1–U3 新顶栏 + 启动按钮）；
> baseline-drift（B1–B3）已执行完毕但**未随 alpha.4 分发**。
> 工作树仅 `handoff/held-untracked/`（vibe_images logo 图）未跟踪。

## 一、已闭环（无需行动）

- alpha.1–alpha.4 四个发布轮全部落账（§9.4–9.7）；
- structural 通道 S1–S5、version-probe V1–V3、UI 精简 U1–U3、基线分类放行 B1–B3 全部执行完毕；
- B 轮门禁：typecheck/lint 0，单测 366，集成 188。

## 二、现存问题清单（按优先级）

### P1-1 真机 1070 条复验（就是你卡住的那次场景）

- B1–B3 的分类放行只在合成夹具上验过；「OpenCode 自动更新后重应用自动重新接管」
  这条修复**必须在你机器上复验**（你机器上的 1.18.29→1.18.31 正是天然场景）；
- 需 jc 当次授权：复验 = 对已更新的安装直接重应用，观察是否出现
  「检测到目标已更新至 1.18.31，已自动更新基线」并放行；
- 未复验前，README 如实写着「alpha.4 不含此修复」——**你机器上的 alpha.4 仍会被基线门卡住**，
  这是你目前最直接的用户体验问题。

### P1-2 README 候选指针不一致（已坐实，下轮顺手修）

- `README.md:183-184` 写「当前候选 `20260922050618-c48863e-769d1d`」（alpha.4，对）；
- `README.md:202-204` 却写「唯一可用于验证与分发的是当前候选 `20260921124125-bd8c2d5-0c200b`」
  （alpha.3 的，**过期**）；同节需一并核对「预存在候选」表述。

### P2-1 e2e:electron B 轮未单跑

- `952708e` 遗留：B1–B3 提交窗口内 e2e:electron 未单独跑过（alpha.4 门禁那次跑过，
  但 B 轮提交在其后）。补跑一次即可闭环，成本几分钟。

### P2-2 `handoff/held-untracked/vibe_images` 处置

- logo 图一直未跟踪挂起；入库（作为 README/发布素材）还是删除，jc 拍板。

### P3-1 下一候选发布轮（让修复真正到用户手里）

- baseline-drift 修复 + B 轮全部改动需打一个新候选（alpha.5 或补丁轮）；
- alpha.4 门禁已证明本会话环境能跑 smoke:gui（27.9s 通过），**不再需要等显示会话机器**；
- 发布仍按老规矩：完整链 ALL_GREEN → 登记 → 核验 → A5 口径 → jc 签核。

### P3-2 长期挂账（不阻塞，逐条销）

| 项 | 状态 |
|---|---|
| U1–U3 真机走查（新顶栏观感 + 启动按钮） | alpha.4 已分发但 jc 尚未实机确认观感 |
| A6/T65 干净环境验证 | 仍只在开发机验过 |
| G1.3 旧候选 `20260918013040` 处置 | 决策仍悬置 |
| 历史候选目录（磁盘 13 个 candidate-* 目录） | 处置待 jc（占用大，删除需授权） |
| P3 外观缺陷（真机色块大小） | 遗留 |
| 演示素材拍摄 | 未做 |
| A7 隐私处置清单（10 文件本机路径/5 截图/49 邮箱提交） | 待 jc 确认 |

## 三、执行顺序建议

1. **现在（本机，无需授权）**：修 README 指针（P1-2）一个提交；补跑 e2e:electron（P2-1）；
2. **你授权后（真机）**：P1-1 复验——这是体验最痛的一项，建议优先；
3. **复验通过后**：启动 P3-1 新候选发布轮，让修复随包发出；
4. 穿插拍板：vibe_images 处置、历史候选目录清理授权。

> 注：P1-1 与 P3-1 有先后关系——复验通过前不打新候选（否则发出的包没经过真机场景验证）。
