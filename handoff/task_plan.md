# OpenCode Theme Switcher：第一版执行计划

状态：计划编制完成；产品开发尚未启动。

## 目标与边界

将本机专用换肤原型改造成可交付的 Windows 桌面工具，完成「选择图片 → 自动配色 → 调整可读性 → 预览 → 应用与恢复」闭环。

- 本轮只编制交接计划，不执行原型脚本、不安装依赖、不修改 OpenCode。
- 新开发仅在用户确认的项目目录内进行。原型保留，不直接覆盖；安装资源写入另设真人确认。
- 不开发账号系统、主题商城、云端图片处理、AI 生图、自动更新、macOS/Linux 适配、任意 CSS/JS 注入、终端主题重写。
- 五项功能的详细任务、接口和验收要求见 `EXECUTION_PLAN.md`。

## 阶段状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| 计划准备 | 读取原型与历史说明，明确风险 | complete |
| 计划交付 | 执行计划、事实清单、交接日志 | complete |
| P0 | 目标识别、可行性和适配路线验证 | complete（只读取证完成，见 docs/discovery.md） |
| P1 | 工程骨架与接口冻结 | complete |
| P2/P3 | 主题生成和安全应用后端 | complete（含物理 I/O 层与事务闭环） |
| P4 | 可视化交互与联调 | complete（实现 + 合成安装 GUI 闭环；**真机视觉走查 T64 待 jc**） |
| P5 | 安全、视觉和打包验收 | partial（安全检查、审计、便携包已做；E2E 已实现并通过；**真机走查与干净环境启动验证待 jc**） |
| P6 | 发布材料和演示交付 | pending（便携包已构建但未签名；演示材料未开始） |
| P5c | 按 P0 审查 R1–R8 的修复与回归 | complete（见 CHANGELOG 与 progress.md 对应小节） |

## 决策

- 安全适配是最先验证的风险，不先画完界面再判断能否应用。
- 优先使用经核实的官方扩展机制；资源补丁仅作为明确标注的受限兼容路线。
- 不把历史记录中的 1.18.29 认定为官方最新版，也不假定其他发行渠道具有相同结构。
- P2、P3、P4 可在 P1 的类型契约冻结后并行；P0、P1、P5、P6 必须按依赖执行。
- 旧的 `task_plan.md`、`progress.md` 属于历史换肤任务，不覆盖；本轮文档单独放在 `handoff/`。
- 用户再次明确目标位置后，交接文档发布到仓库内的 `handoff/`；不存在时创建，仅新增文档，不覆盖原型。

## 错误与阻塞记录

| 事项 | 尝试 | 处理 |
|---|---|---|
| 本轮读取此前 D 盘复制目标时提示目录不存在 | 1 | 不重建、不覆盖用户可能移动的文件；先在源项目的 handoff 目录交付计划 |
| 会话恢复脚本没有输出 | 1 | 不推断恢复成功；以已读取的源码、历史日志和本轮结果为依据 |
| Electron 在无显示会话下 GPU 进程反复重启、`app.exit()` 被拖住 3 分钟 | 1 | 截图与 fixture 工具改为 `process.exit()`（结果同步落盘，无需 flush），并统一 `--disable-gpu --no-sandbox --in-process-gpu` 等开关 |
| 真机 `snow-theme.css` 的内容与 HTML 里的标记不一致（被后续主题覆盖过） | 1 | legacy-theme 把「来源标记」与「内容指纹」分开判定：标记证明来源，内容用来说明撤下的是哪一套；两者不一致不算未知来源 |

## 更新约定

接手 agent 开工前读取本文件、`findings.md`、`progress.md` 和 `EXECUTION_PLAN.md`。每阶段结束记录改动文件、命令、退出码、测试证据及未完成项；不能把「代码写完」标成「验收通过」。
