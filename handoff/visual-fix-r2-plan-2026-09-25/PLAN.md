# 观感修复第二轮实施计划（X1–X3）

- 日期：2026-09-25
- 基线：`5ef422f`（工作树干净；alpha.8 候选 `20260925025400-047ce1f-d4519a` 已登记待发布，本轮改动**不进**该候选，随下一候选生效）
- 来源：jc 三条要求（下拉菜单做深不透明、可读性检查面板折叠或选项卡、封面标语与输入框降透明）
- 执行规则：完成一项 git 提交一次；本文件只列计划，未授权不动手

## 三条要求的定性

| # | 要求（原文要点） | 定性 | 作用层 |
|---|---|---|---|
| 1 | 下拉菜单颜色做深、不做透明，文字看得清 | 菜单/弹层与对话框共用同一半透明面板 alpha（`css.ts:170-186` 统一 `rgba(tokens.panel, panel)`），盖在壁纸上发闷 | `core/theme/css.ts`（真实注入层）+ mock 同步 |
| 2 | 可读性检查面板：折叠显示，或在上方加选项卡切换 | 纯 UI 信息架构：面板在「检查」选项卡里整张铺开，需更省空间 | `renderer/App.tsx` `ContrastPanel.tsx` `styles.css` |
| 3 | 封面 opencode 标语和输入框透明度低一些（更不透明），文字明显看得清 | 封面（空会话首页）无专门规则：标语区无底色兜底、封面输入框共用半透明面板 alpha | `core/theme/css.ts`（需真机 DOM 取证） |

## 已核实的现状

- 菜单组选择器已存在（`css.ts:170-186`：`menu-v2-content` / `dropdown-menu-content` / `context-menu-content` / `tooltip` / `session-tab-popover` 等），但与 dialog、prompt-input **共用** `panel` alpha——修菜单要把它们从这组里拆出来，不能整组改（输入框会被误伤）。
- 「检查」选项卡当前只装 `ContrastPanel` 一个面板（`App.tsx:694-697`）；28 项明细已是 `<details>` 折叠态，用户要的是**面板级**的进一步收纳。
- `css.ts` 无任何封面/标语（logo/slogan/home）选择器——X3 必须走 W1 同款真机取证流程（1.18.32 实际 DOM classList 证据），不能凭猜写选择器。

## 任务总表

| 任务 | 内容 | 依赖 | 真机 |
|---|---|---|---|
| X1 | 下拉菜单不透明化（做深） | — | 需（验证） |
| X2 | 可读性检查面板折叠收纳 | — | 否 |
| X3 | 封面标语与输入框降透明 | — | 需（取证+验证） |

## X1：下拉菜单做深不透明（一提交）

**步骤**：
1. 把菜单/弹层选择器（`menu-v2-*`、`dropdown-menu-*`、`context-menu-*`、`session-tab-popover`；tooltip 一并评估）从 `css.ts:170-186` 共享组拆出，单列一组规则；
2. 菜单面改用**不透明深色面**：alpha=1（或 ≥0.97 的近实色），底色取 panel 色或按 mode 再加深/提亮一档（`generate.ts` 已有 lighten/darken 工具）；border 保持 0.9；
3. 对比度自证：菜单文字（`tokens.text`）对新底色的对比度纳入 `report.ts` 条目模型或至少单测断言 ≥4.5——不能只改不测；
4. mock 预览如有菜单示意则同步（无则在预览注释里说明菜单不在预览覆盖范围）；
5. 验收：单测断言菜单规则输出实色（rgba alpha=1）且对比达标；typecheck/lint/unit 全绿；真机打开 Build/模型选择器、会话标签弹层截图确认文字清晰。

**边界**：dialog 与 prompt-input 留在原组不动（它们的半透明是 W1/W2 定过的观感）；只拆菜单类。

## X2：可读性检查面板折叠收纳（一提交）

**方案（默认采 A，B 备选）**：
- **A. 面板级折叠（默认）**：面板默认收成一行结论条——「✓ 全部条目达标（估算）· 最差 错误提示 4.66/4.5」或「✗ N 项未达标」；点结论条展开整卡（代表性底色、最差项、28 项明细的现有结构原样保留在内）。未达标时**默认展开**（需要用户看到问题），全达标时默认收起。
- B. 子选项卡：「检查」内再分「概要/明细」——多一次点击，信息 scent 差，不采，除非 jc 点名要。

**步骤**：
1. `ContrastPanel.tsx` 加面板级折叠（原生 `<details>` 或受控展开，样式与现有「明细与判定依据」一致）；
2. e2e 选择器核对：`.entries li` 等断言在深层 details 内，面板级折叠后需同步打开或调整（逐个 grep 确认，能不改不改）；
3. 验收：typecheck/lint/unit/e2e 相关用例全绿；截图确认默认态一行、点击展开完整。

**边界**：只动呈现，报告数据与判定逻辑一行不动。

## X3：封面标语与输入框降透明（一提交）

**步骤**：
1. **取证（先行，只读）**：真机 1.18.32 封面（空会话首页）取 DOM 证据——opencode 大字标语容器的组件标识、封面输入框与会话内输入框是否同一组件（`dock-prompt`/`prompt-input` 已在半透明组里，封面若是同一组件，规则要按「封面上下文」限定，不能全局加深误伤会话内观感）；证据按 W1 先例写 `handoff` 证据文件；
2. 修复：
   - 封面输入框：在封面上下文中改近实色（alpha≈0.95–1），文字对比自证；
   - 标语区：保证文字清晰——优先给标语容器一个同源实色/近实色底，或按取证结果选更贴官方结构的方案；
3. mock 预览同步：预览首页示意按同一结论改，保持同构；
4. 验收：单测覆盖新规则；真机封面截图前后对比，文字明显可读。

**边界**：选择器必须来自取证证据，不猜；会话内输入框观感不被封面规则误伤（上下文限定）。

## 执行顺序

1. **X2**（纯本机，无依赖，先落地）；
2. **X1 → X3** 攒同一个真机窗口：X3 取证 → X1/X3 改码 → 一次应用全部验证（菜单、封面、回归确认会话内输入框无变化）；
3. 全部完成后随下一候选（alpha.9 轮）发布，不动已登记的 alpha.8。

## 全局边界

- 每项一提交；提交信息如实写明验证层级；
- token 与 alpha 同源红线不动；report 条目模型改动需同步 mock（同 F4c 口径）；
- 真机验证需 jc 在场（应用/恢复授权、截图确认）；
- 文档同步：X1/X3 若改默认观感写 CHANGELOG 未发布段。
